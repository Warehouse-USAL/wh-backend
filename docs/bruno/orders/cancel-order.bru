meta {
  name: Cancel Order
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/orders/:id/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: ORDER_ID_HERE
}

params:query {
  ~reason: Cancelled by operator
}
